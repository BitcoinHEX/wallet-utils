const stake = {
    startStake: (newStakedHearts, newStakedDays) => {},
    goodAccounting: (stakerAddr, stakeIndex, stakeIdParam) => {},
    endStake: (stakeIndex, stakeIdParam) => {},
    getStakeCount: (ethAddr) => {},
};

module.exports = stake;
